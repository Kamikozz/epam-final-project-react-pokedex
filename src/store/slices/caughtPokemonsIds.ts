// import { AppState, Action } from "../../reducer";

// const initialState: null | Set<number> = null;

// export default function caughtPokemonsIdsReducer(state = initialState, action: Action) {
//   switch (action.type) {
//     default:
//       return state;
//   }
// };

// export const selectCaughtPokemonsIds = (state: AppState) => state.caughtPokemonsIds;
export {

};
