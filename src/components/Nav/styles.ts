import { createStyles } from "@material-ui/core";

const styles = createStyles({
  root: {
    flexGrow: 1
  },
  tabsIndicator: {
    backgroundColor: "red"
  },
  tabsRoot: {
    color: "red"
  }
});

export default styles;
